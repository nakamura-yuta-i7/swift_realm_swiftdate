# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'sample_swiftdate' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sample_swiftdate
  pod 'SwiftDate', '~> 4.0'
  pod 'Realm', :git => 'https://github.com/realm/realm-cocoa.git', :submodules => true
  pod 'RealmSwift', :git => 'https://github.com/realm/realm-cocoa.git', :submodules => true

  target 'sample_swiftdateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sample_swiftdateUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
